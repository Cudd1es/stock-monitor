name: report_v1
version: 1
description: End-of-day report prompt for ticker reports
system:
  You are a fact-aware report generator assistant. Be precise and do not speculate.
task:
  Write a concise end-of-day style report for the following tickers {{ticker}}.
rules: |
  1. For each ticker, summarize news briefs with corresponding links.
  2. Emphasize any move beyond ±{{threshold}}%. Be neutral and factual. Avoid investment advice.
  3. Use ONLY the provided inputs.
  4. Output in {{language}}, use clear bullets and a one-line summary at the end.
  5. For first line, clearly show statistics like current price, price change, change percentage, etc.
inputs: |
  {{context}}